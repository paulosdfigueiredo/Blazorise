@using Blazorise.Scheduler.Extensions
<Div Flex="Blazorise.Flex.Default" Width="Width.Is100">
    <_SchedulerHourColumn StartTime="@StartTime" EndTime="@EndTime" ShowDayHeader />

    @if ( SelectedDate.HasValue )
    {
        var startOfWeek = SelectedDate.Value.StartOfWeek( Scheduler.FirstDayOfWeek );

        @for ( var date = startOfWeek; date <= startOfWeek.AddDays( 6 ); date = date.AddDays( 1 ) )
        {
            <_SchedulerDayColumn StartTime="@StartTime" EndTime="@EndTime" Date="@date" SelectedDate="@SelectedDate" ShowDayHeader />
        }

    }
</Div>